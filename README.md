# نظام تارجت للخدمات الحكومية (Target Government Services)

تطبيق متكامل لإدارة الخدمات الحكومية، المصروفات، والمديونيات، مصمم ليعمل كمنصة ويب وتطبيق أندرويد باستخدام React و Capacitor.

## ✨ المميزات الرئيسية

- **لوحة التحكم (Dashboard):** عرض إحصائيات الخزنة اليومية، المصروفات، والمديونيات في الوقت الفعلي.
- **إدارة العمليات:** تسجيل المعاملات الحكومية (بطاقات، جوازات، شهادات ميلاد) مع دعم الباركود التلقائي.
- **نظام الإلغاء الذكي:** إمكانية إلغاء العمليات مع خيارات استرداد كامل أو خصم مصروفات إدارية.
- **إدارة المديونيات (Receivables):** تتبع الديون والتحصيل الجزيئي مع تحديث الأرصدة تلقائياً.
- **سجل المصروفات:** تتبع المصروفات اليومية وربطها بالعملاء والموردين.
- **التقارير المالية:** تقارير مفصلة لكل فرع مع إحصائيات دقيقة للأرباح والتحصيل الإلكتروني.
- **نظام المخزن:** إدارة باركود البطاقات وتوزيعها على الفروع.

## 🚀 التقنيات المستخدمة

- **Frontend:** React + Vite + TypeScript.
- **Styling:** Vanilla CSS + Tailwind (Utility components).
- **Backend:** Google Apps Script (Google Sheets side).
- **Mobile:** Capacitor (Android).
- **PDF:** jsPDF لإنشاء إيصالات العملاء.

## 🛠️ خطوات التشغيل

### 1. المتطلبات
- Node.js (v18+)
- حساب Google (لاستخدام Google Sheets كقاعدة بيانات).

### 2. إعداد قاعدة البيانات (Google Sheets)
- قم بإنشاء ملف Google Sheets جديد.
- افتح `Extensions` -> `Apps Script`.
- انسخ محتويات ملف `backend.gs` الموجود في هذا المشروع والصقها في محرر Apps Script.
- قم بعمل `Deploy` -> `New Deployment` كـ `Web App`.
- اجعل الصلاحيات لـ `Anyone`.
- انسخ الرابط (URL) الناتج وضعه في ملف `src/services/googleSheetsService.ts` في متغير `GOOGLE_SCRIPT_URL`.

### 3. التشغيل المحلي
```bash
# تثبيت المكتبات
npm install

# تشغيل نظام التطوير
npm run dev
```

### 4. البناء للإنتاج (Production Build)
```bash
npm run build
```

## 📱 الأندرويد (Capacitor)
لبناء ملف APK:
```bash
# إضافة منصة أندرويد
npx cap add android

# مزامنة الكود
npx cap sync

# فتح المشروع في Android Studio
npx cap open android
```

---
تم التطوير بواسطة Antigravity لمشروع تارجت.
